# Shared kernel code — placeholder for Phase 2.
#
# This directory will contain the architecture-independent kernel entry path,
# console abstraction (write and panic), and any shared formatting utilities.
# No architecture-specific code, port I/O, MMIO, or register manipulation
# belongs here.
