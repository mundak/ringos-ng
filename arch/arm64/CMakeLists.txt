# arm64 architecture-specific code — placeholder for Phase 4.
#
# This directory will contain the arm64 boot path (QEMU virt direct kernel
# entry), stack initialization, BSS clearing, and the PL011 UART driver.
# Control transfers to the shared kernel entry after setup.
