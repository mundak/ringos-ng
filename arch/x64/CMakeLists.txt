# x64 architecture-specific code — placeholder for Phase 3.
#
# This directory will contain the x64 boot path (Multiboot2-compatible entry),
# GDT and long-mode setup, stack initialization, BSS clearing, and the COM1
# serial driver. Control transfers to the shared kernel entry after setup.
